#pragma once
/******************************************************************
*Note:这个头文件是为了生成Lib文件引用接口的头文件
*在使用Lib库是只包含这个头文件就可以了.
*尽量减少用户需要注意的细节,让用户操作更方便.
* 
* 设计原则：
* 1.计算与IO分离。
* 2.必须写测试项目。
* 3.测试的成果也可以提供给使用者。
********************************************************************/



#include "preliminary.h"
#include "signal.h"
